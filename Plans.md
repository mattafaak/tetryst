# tetryst Plans.md

Created: 2026-05-25

---

## Phase 1: Core gameplay bugs

| Task | Content | DoD | Depends | Status |
|------|---------|-----|---------|--------|
| 1.1 | **Fix Sprint elapsed time tracking** — `modeTimer` stays at 0 in Sprint mode because only Ultra counts down. Add `modeTimer += dt` in `updatePlaying` for Sprint, and verify the Victory screen / high scores display the correct completion time. | Sprint completion time > 0 on victory; high scores save and display correct time; existing test suite passes | - | cc:完了 |
| 1.2 | **Fix B2B popup on first eligible clear** — `buildPopups` receives `scoreResult.isB2B` which is already true after the first Tetris. Pass `scoreResult.isB2B && wasB2BActive` instead so "BACK-TO-BACK" only shows on consecutive eligible clears. | No "BACK-TO-BACK" popup on the first Tetris; popup still shows on second consecutive Tetris; existing tests pass | - | cc:完了 |

## Phase 2: Code cleanup

| Task | Content | DoD | Depends | Status |
|------|---------|-----|---------|--------|
| 2.1 | **Deduplicate `checkCollision` in gravity.ts** — Delete the local `checkCollision` function and import it from `board.ts` instead. | gravity.ts uses the shared `checkCollision` from board.ts; all tests pass | - | cc:完了 |
| 2.2 | **Deduplicate `checkPerfectClear`** — There are two identical board-scanning implementations (actions.ts and loop.ts). Extract to a shared location (e.g., board.ts) and have both callers use it. | Single `checkPerfectClear` in board.ts; actions.ts and loop.ts both import it; tests pass | - | cc:完了 |
| 2.3 | **Remove dead code** — Clean up unused exports: `addSoftDropScore` (scoring.ts), `onPieceLanded`/`resetLockTimer` (lock-delay.ts), `transitionPhase`/`recalculateGhostY` (state.ts), `initAudio` (sfx.ts & music.ts), `toggleMusic`/`isMusicPlaying` (music.ts), `createCPUAIController` (ai-controller.ts). | No build warnings about unused exports; all tests pass | - | cc:完了 |

## Phase 3: Automated deep bug-testing

| Task | Content | DoD | Depends | Status |
|------|---------|-----|---------|--------|
| 3.1 | **Install testing infrastructure** — Add fast-check (property-based fuzzing), playwright (browser E2E). Create shared test helpers: `asciiBoard()` for ASCII-art board representation (proven effective by tdd-tetris-tutorial and openblok), `emptyBoard()` for clean 40×10 grid, `fillRows()` helper, `randomBoard()` generator for fuzz tests. Configure Playwright for headless Chrome. | `npm test` passes; `npx playwright install` completes; helper functions importable from test files; all 3 tools install cleanly | - | cc:完了 |
| 3.2 | **Property-based fuzz: collision + board logic** — Fuzz `checkCollision` with random piece types, positions, rotations, and board states. Cross-project bug pattern: S/Z pieces clipping ceiling when rotated flat on ground (floor kick failure). Fuzz `clearLines` with boards having random fill patterns — cross-project bug: concurrent line deletion loop not handling multiple rows (mauricekastelijn/tetris #14). Fuzz `lockPiece` verify idempotency (no cell overlap after lock). Fuzz `isLockOut` at every y=0..39 for every piece — cross-project bug: off-by-one in buffer/visible boundary. Fuzz `isPerfectClear` with boards of varying emptiness. Use fast-check `fc.property` with `fc.constantFrom` for piece types and custom board generators. | ≥200 fuzz scenarios across board.ts functions; no invariant violated for 1000+ random iterations each; all existing tests pass | 3.1 | cc:完了 |
| 3.3 | **Property-based fuzz: SRS wall kicks + ghost piece** — Fuzz `tryRotateCW`/`tryRotateCCW` at every x=0..9, y=0..39, all 4 rotations, for JLSTZ and I pieces. Cross-project bug pattern: I-piece using JLSTZ kick table instead of its own (most common SRS bug). Fuzz wall kick coverage: every kick test (all 5 per rotation transition, both tables) should be reachable by some board config. Fuzz `getGhostY`: invariant — ghost position is the lowest valid placement reachable by vertical movement only; cross-project bug: ghost piece offset 1 row up (harddrop.com tetris.com bug). Fuzz collision-after-rotation invariant: no rotation returning non-null produces a piece where `checkCollision` is true. Use table-driven approach (tetrigo/openblok pattern) for individual kick test verification. | Every SRS kick transition covered; 1000+ fuzz iterations across piece×position×rotation; `checkCollision(rotatedPiece) === false` invariant holds for all successful rotations; all existing tests pass | 3.1 | cc:完了 |
| 3.4 | **Property-based fuzz: scoring + T-spin detection** — Fuzz `detectTSpin` over all 4 rotations: generate random 3×3 corner occupancy (2^4=16 patterns × 4 rotations = 64 total). Verify 3-corner rule. Cross-project bug: out-of-bounds corners at board edges treated as occupied (false positive). Fuzz `evaluateClear` with random combinations — cross-project bug: back-to-back breaking on no-clear lock (should NOT break); perfect clear score replacing base score instead of adding to it. Invariant: score ≥ 0 and monotonic with inputs; B2B flag coherent. Fuzz `effectiveLinesFor` — invariant: B2B ≥ non-B2B for same inputs. Fuzz combo chain (`updateCombo`): 0→255 consecutive clears — cross-project bug: combo counter off-by-one (first clear should award 0 combo bonus = count starts at -1). | 500+ fuzz scenarios across scoring.ts + combo.ts; all known cross-project bug patterns tested; invariants hold for 1000+ random iterations; all existing tests pass | 3.1 | cc:完了 |
| 3.5 | **Property-based fuzz: AI brain heuristics** — Fuzz `evaluateBoard` with randomly generated board states (various fill patterns, column heights, hole configurations). Fuzz `computeColumnHeights` — invariant: sum of heights ≤ BOARD_WIDTH × BOARD_HEIGHT. Fuzz `countHoles` — invariant: holes ≤ BOARD_WIDTH × BOARD_HEIGHT, holes never exceed position-relative emptiness. Fuzz `computeBumpiness` — invariant: result is non-negative. Fuzz `generatePlacements` for all 7 piece types on boards with random debris — invariant: every placement is a valid non-colliding position. Cross-project lesson: no open-source Tetris project tests AI heuristics with property-based approaches at all — this is novel coverage. | ≥100 fuzz scenarios across ai-brain.ts; all invariants hold for 1000+ random iterations; generatePlacements produces valid placements only; all existing tests pass | 3.1 | cc:完了 |

## Phase 4: Input system + game loop testing

| Task | Content | DoD | Depends | Status |
|------|---------|-----|---------|--------|
| 4.1 | **DAS/ARR input system tests** — Unit test `KeyboardHandler` with controlled timer increments. Cross-project bug patterns: (1) first ARR repeat firing too early (immediately with initial press, skipping DAS delay), (2) DAS not resetting between pieces (new piece inherits partial DAS charge from previous), (3) soft drop clearing DAS for horizontal movement. Scenarios with manual dt injection: initial press fires immediately; DAS delay tracked correctly; first ARR repeat fires exactly after DAS charges; subsequent repeats at ARR_RATE spacing; non-DAS keys fire exactly once per press; multi-key (hold left, press right, release left); reset clears all state. Fix: non-DAS keys were incorrectly processed by DAS/ARR path (would repeat after delay) — added `!(code in DAS_ACTIONS) continue` guard in update(). | ≥20 test cases covering all DAS/ARR state transitions; cross-project bug patterns covered; non-DAS repeat bug fixed; all existing tests pass | 3.1 | cc:完了 |
| 4.2 | **Game loop integration: full mode simulations** — Integration test the `Game` class (loop.ts) using a mock CanvasRenderingContext2D. Novel in open-source Tetris: no project tested full game-loop integration (all known projects test individual mechanics only). Scenarios: (a) AI-controlled marathon attract mode plays continuously, (b) menu → mode selection (ArrowLeft/Right cycles through Marathon/Sprint/Ultra), (c) start level selection (ArrowUp/Down), (d) Sprint modeTimer tracks elapsed time, (e) Enter from menu starts a game, (f) attract mode entry/exit, (g) mute toggles, (h) restart from GameOver. Uses mock requestAnimationFrame, performance.now(), window stubs. | 14 integration tests passing; menu navigation, attract mode, mode selection, and phase transitions verified; all existing tests pass | 4.1, 3.2, 3.3, 3.4 | cc:完了 |
| 4.3 | **Playwright browser E2E tests** — Install and configure Playwright with config, webServer, and 8 E2E test cases. Tests: page loads without console errors; canvas renders; Enter starts a game; arrow keys cycle modes; page title correct; rapid inputs don't crash; mute key works; renders at 800×600 and 400×700 viewports. Run via `npm run test:e2e`. | 8 E2E tests against built Vite dev server; no console errors; runnable via `npm run test:e2e` | 3.1 | cc:完了 |
| 4.4 | **Comprehensive edge-case stress tests** — 37 edge-case/invariant tests informed by cross-project bug patterns: (a) all perfect-clear permutations (every line count + PC combo), (b) lock-delay 15-reset boundary — lock forced after max resets (infinity exploit prevention, TDG §7), (c) combo chain 0→254 with expected score accumulation, (d) spawn collision for I/T/O pieces (block-out coverage), (e) hold-cycle over full bag, (f) gravity timer edge values (dt=0, dt=999, dt=1000, dt=1500), (g) next-queue consistency across 10+ spawns, (h) 7-bag boundary: unique per bag, first-bag restricted set, distribution uniformity, (i) lock-out at exact buffer boundary for all piece types, (j) concurrent non-contiguous line clears. | 37 edge-case tests passing; infinity exploit and block-out patterns covered; all boundary conditions correct; all existing tests pass | 3.2, 3.3, 3.4 | cc:完了 |

## Phase 5: Critical bug fixes [tdd:required]

| Task | Content | DoD | Depends | Status |
|------|---------|-----|---------|--------|
| 5.1 | **Fix attract mode freeze on GameOver** — `updateAttractGame` has a `GameOver`/`Victory` case (lines 458-463) that is structurally unreachable; the phase switch in `update()` hits the no-op case before `updateAttractGame` is called again. Fix: move auto-restart logic to `update()` phase switch, or restructure the phase dispatch so attract mode catches its own terminal states. Verify attract mode cycles indefinitely. | Attract mode plays 3+ full games without freezing; no regression in player mode; all tests pass | - | cc:完了 |
| 5.2 | **Refactor duplicate scoring/lock logic** — `handleHardDrop` (actions.ts:135-243) and `lockActivePiece` (loop.ts:307-413) implement the same scoring/lock sequence independently. Hard drops miss: all SFX (lock, clear, tspin, levelup), all action popups (TETRIS, T-SPIN, PERFECT CLEAR, B2B, COMBO), level-up popup, mode victory check. Extract shared logic into a pure function in a new module (e.g., `core/lock.ts`) that returns a delta record. Both callers apply the delta. | Hard drops show popups and play SFX identical to gravity locks; no code duplication between the two paths; all tests pass | - | cc:完了 |
| 5.3 | **Fix Victory→Start attract mode dump** — Pressing Start on Victory screen sets `menuIsStatic = false` (loop.ts:137-141), which triggers `startAttractMode()` on the next frame. Fix: set `menuIsStatic = true` so the user returns to the static menu where they can select mode/level. | Victory→Start returns to static menu with mode/level selection; pressing Start again starts a game; all tests pass | - | cc:完了 |
| 5.4 | **Fix Ultra timer pausing on line clears** — Ultra `modeTimer` only decrements in `updatePlaying` (loop.ts:267-275). During `LineClear` and `EntryDelay` phases, the timer stalls, giving the player extra seconds. Fix: move Ultra timer countdown to `update()` so it runs every frame regardless of phase, or accumulate elapsed time in a dedicated variable outside the phase switch. | Ultra timer counts down continuously (including during line-clear animation and entry delay); Sprint timer unaffected; all tests pass | - | cc:完了 |

## Phase 6: Rendering + audio tests

| Task | Content | DoD | Depends | Status |
|------|---------|-----|---------|--------|
| 6.1 | **Canvas rendering screenshot tests** — Add Playwright screenshot comparison tests for key game states: menu screen, active gameplay (AI vs random board), game-over screen, victory screen, pause overlay. Use Playwright's built-in screenshot capture. Store reference screenshots in a `e2e/screenshots/` directory. Tests should verify the canvas draws recognizable layouts (not blank). Consider using `toMatchSnapshot()` for pixel-level comparison or manual visual inspection of captured output. | 5+ screenshot tests capturing distinct game states; reference images committed; tests runnable via `npm run test:e2e` | 3.1 (Playwright infra) | cc:完了 |
| 6.2 | **Audio module unit tests** — Write unit tests for all three audio modules. For `audio-ctx.ts`: test lazy singleton creation, resume-on-suspended, same-context-on-repeated-call. For `sfx.ts`: test that each of the 9 SFX types schedules an oscillator with the correct frequency/duration parameters (mock AudioContext). For `music.ts`: test `freqOf` resolves all 176 notes, `bassFreqOf` resolves roots correctly, `buildSchedule` produces correct event count (176) and correct beat offsets, `stopMusic` cleans up active oscillators. | ≥20 audio unit tests; all 176 melody notes resolve to valid frequencies; buildSchedule produces 176 events with correct timing; all tests pass | - | cc:完了 |
| 6.3 | **Game loop edge-case tests** — Tests for untested game loop paths: pause/resume cycle (verify gravity stops, input blocked, resumes correctly), Ultra timer countdown (verify modeTimer reaches 0), multiple rapid restarts (check no state leaks), keyboard DAS reset between games, mute toggle state tracking, LineClear animation phase timing, Victory phase transition from mode completion. | ≥10 new edge-case loop tests; pause/resume, restart, Ultra timer verified; all tests pass | 4.2 (loop tests infra) | cc:完了 |

## Phase 7: Low-impact bug fixes

| Task | Content | DoD | Depends | Status |
|------|---------|-----|---------|--------|
| 7.1 | **Fix unhandled AudioContext resume Promise** — `audioCtx.resume()` returns a Promise that is not awaited or caught (audio-ctx.ts:11). If the browser rejects the resume (autoplay policy), the error is swallowed silently and a suspended AudioContext may be returned to callers. Fix: attach `.catch(() => {})` to suppress the rejection, and consider returning null to callers if the context is still suspended after resume attempt. | Audio playback does not throw unhandled Promise rejections; SFX gracefully degrades if AudioContext stays suspended; all tests pass | - | cc:完了 |
| 7.2 | **Fix attract mode ignoring selectedMode** — `startAttractMode()` calls `startGame(createInitialState())` which defaults to Marathon (loop.ts:503). If user selected Sprint/Ultra, attract AI plays Marathon anyway. Fix: pass `this.selectedMode` through to `createInitialState`. | Attract mode plays the mode the user had selected; all tests pass | - | cc:完了 |
| 7.3 | **Fix keyboard DAS accumulation during pause** — `keyboard.update(dt)` runs before the phase switch (loop.ts:207). DAS/ARR timers accumulate while paused, causing instant-fire on unpause. Fix: guard the keyboard update with a pause check, or reset DAS timers on pause entry. | Holding a direction key while pausing does not cause instant input on unpause; all tests pass | - | cc:完了 |
| 7.4 | **Fix inconsistent bag-refill syntax** — `startGame` uses `[...currentBag, ...createBag()]` (state.ts:54) while `spawnFromQueue` uses `createBag()` (state.ts:150). Semantically equivalent now but would diverge if `drawFromBag` changed its `needsNewBag` threshold. Fix: make both use the same pattern (append is safer). | Both bag-refill sites use identical syntax; all tests pass | - | cc:完了 |
| 7.5 | **Add input guard for Paused phase** — `handleInput` has no explicit guard for `GamePhase.Paused` (loop.ts:77-171). Safety relies on every downstream action handler checking phase. Fix: add a `case GamePhase.Paused:` that only accepts Pause action, before the `processAction` dispatch. | Paused phase only accepts Pause action; all tests pass | - | cc:完了 |

## Phase 8: Performance optimizations

| Task | Content | DoD | Depends | Status |
|------|---------|-----|---------|--------|
| 8.1 | **Offscreen canvas caching for board + grid** — The locked board (200 cells × 3 fill ops = 600 fills/frame) and grid lines (31 stroked lines/frame) are static between lock/clear events (~2-3×/second) but redrawn from scratch at 60fps. Fix: render locked board + grid onto an offscreen canvas once, then `ctx.drawImage()` each frame. Re-render offscreen canvas only on `lockPiece` or `clearLines`. Estimated impact: eliminates ~95% of per-frame fill operations. | Offscreen canvas renders board + grid once per lock/clear; `drawImage` used in hot path; no visible rendering quality regression; all tests pass | - | cc:完了 |
| 8.2 | **Cache cellSize and skip per-frame layout reads** — `calculateCellSize()` reads `devicePixelRatio`, `canvas.width`, `canvas.height` (layout-triggering) every frame (loop.ts:191). Cells size only changes on window resize. Fix: compute `cellSize` once in `start()` and update from a `resize` listener. Store result and skip per-frame call. | `calculateCellSize()` called only on resize, not per frame; rendering still responsive to window resize; all tests pass | - | cc:完了 |
| 8.3 | **Zero-allocation hot paths** — Three high-frequency functions allocate excessive temporary objects. Fix: (a) `applyGravity` (gravity.ts:36-52) creates new `Piece` per drop tick — inline collision check in the while-loop body to avoid per-tick allocations; (b) `getGhostY` (pieces.ts:100-114) creates new `Piece` per iteration — inline collision check against board directly; (c) `tickPopups` (popups.ts:14-18) allocates 2-3 arrays per frame — mutate popup timers in-place since they are ephemeral. | Hot paths produce no intermediate Piece/Position allocations in normal operation; all tests pass | - | cc:完了 |
| 8.4 | **Batch mode-timer and lock-delay state spreads** — Ultra/Sprint mode creates `{...this.state, modeTimer: ...}` every frame (loop.ts:268-280) and lock-delay creates `{...state, lockState: {...state.lockState, timer: ...}}` every grounded frame (lock-delay.ts:20-25). Fix: defer these timer updates to the next frame's immutable state mutation (e.g., fold into `applyGravity`'s state spread), or use a mutable frame-local accumulator that gets applied once per frame. | Single GameState spread per frame instead of 2-3; no behavioral change observable in gameplay; all tests pass | - | cc:完了 |
| 8.5 | **Cache buildSchedule result** — `buildSchedule(song)` is called every ~73s (each `playSongCycle`), creating 176 `NoteEvent` objects each time from a compile-time constant melody. Fix: compute once at module init and cache. | `buildSchedule` called at most once; cached schedule reused on loop; all tests pass | - | cc:完了 |
| 8.6 | **Set Vite build.target to ES2023** — `build.target` defaults to `"modules"`. Setting it to `"es2023"` (matching tsconfig) lets esbuild emit native modern syntax, slightly reducing bundle size and improving parse time. | vite.config.ts sets `build.target: "es2023"`; build outputs functionally identical; all tests pass | - | cc:完了 |

## Phase 9: Follow-up bug fixes

| Task | Content | DoD | Depends | Status |
|------|---------|-----|---------|--------|
| 9.1 | **Fix Ultra timer spam calling triggerVictory every frame** — Once modeTimer reaches 0, `updateModeTimer` fires `triggerVictory()` every subsequent frame, hammering `saveHighScore()` with redundant localStorage writes. Fix: add phase guard so it only fires once. | Ultra timer expiry triggers victory once; no redundant saveHighScore calls; all tests pass | 5.4 | cc:完了 |
| 9.2 | **Fix missing victory check on no-line-clear lock path** — `executeLock` only checks `checkModeVictory` when lines are cleared. T-spin no-clears can raise Marathon level to max, but victory is never triggered on that path. Fix: add victory check in the else branch. | Marathon victory via T-spin no-clear level cap works correctly; all tests pass | 5.2 | cc:完了 |
| 9.3 | **Fix latent crash in getGhostY inline collision** — The inlined collision check accesses `board[by][bx]` without a `by >= 0` guard. If a piece's Y goes negative (extreme kick), this throws TypeError. Fix: add guard matching `checkCollision`'s contract. | getGhostY handles negative Y coordinates safely; all tests pass | 8.3 | cc:完了 |

## Phase 10: Pause menu + high score screen

| Task | Content | DoD | Depends | Status |
|------|---------|-----|---------|--------|
| 10.1 | **Add ShowHighScores input infrastructure** — Add `{ type: "ShowHighScores" }` to `InputAction`, map `KeyH` in keyboard.ts, no-op handler in actions.ts | H key fires ShowHighScores; processAction accepts it | - | cc:完了 |
| 10.2 | **Implement pause menu** — Replace bare `drawPause` overlay with a 3-option menu (Resume/Restart/Quit to Menu). Track selection in Game class. ↑↓ navigates, Enter selects. | Pause shows selectable options; Restart/Quit work; all tests pass | 10.1 | cc:完了 |
| 10.3 | **Implement high score screen** — New `drawHighScores` overlay accessible from menu via H key. Shows top 10 scores per mode with mode tabs (←→ to switch). Any key dismisses. | H toggles high scores from menu; ←→ cycles mode; any key dismisses | 10.1 | cc:完了 |
| 10.4 | **High score screen mode switching** — Track `highScoreMode` separately from `selectedMode` so browsing scores doesn't change the player's selected game mode. | ←→ on high score screen cycles display without changing menu mode selection | 10.3 | cc:完了 |
| 10.5 | **Tests** — 12 new tests: pause navigation, resume/restart/quit, selection wrap, high score toggle and mode cycling | 12 tests pass (670 total); all existing tests pass | 10.2, 10.4 | cc:完了 |

## Phase 11: Visual polish + developer experience

| Task | Content | DoD | Depends | Status |
|------|---------|-----|---------|--------|
| 11.1 | **Background visual effects** — Subtle animated star field behind the game board using a small offscreen canvas. Minimal CPU (<1%), runs continuously. | Background renders with visible non-black particles; no frame rate degradation vs baseline; all tests pass | - | cc:完了 |
| 11.2 | **Enhanced line-clear particle effects** — Replace simple white-flash with colored particle burst. Small squares scatter from cleared rows and fade out over the line-clear animation duration. | Particles render during line clear and fade out; no performance regression; all tests pass | - | cc:完了 |
| 11.3 | **Score event screen flash** — Brief full-screen color tint on Tetris (white), T-Spin (yellow), Perfect Clear (rainbow). Duration <200ms, subtle opacity. | Major scoring events trigger brief screen flash; flash is visible but not overwhelming; all tests pass | - | cc:完了 |
| 11.4 | **CI: run tests** — Add `npm test` to GitHub Actions workflow after the build step. | CI workflow includes test step; workflow succeeds on master; PRs are validated | - | cc:完了 |
| 11.5 | **Pause on window blur** — Add `document.addEventListener("visibilitychange", ...)` to auto-pause when tab loses focus. | Tab switch auto-pauses; game resumes on return; all tests pass | - | cc:完了 |
| 11.6 | **180° rotation** — (deferred) | deferred | - | deferred |

## Phase 12: Critical bug fixes [tdd:required]

| Task | Content | DoD | Depends | Status |
|------|---------|-----|---------|--------|
| 12.1 | **Fix Ultra victory path overwrite** — `triggerVictory()` in `tickPlayingState` and `applyUltraTimer` both fire but the return value spreads from the original `state` parameter (which has the old phase), and the caller assigns that return to `this.state`, overwriting Victory back to Playing. `saveHighScore` is called every frame. Fix: move victory detection to the caller after state reassignment, or have the timer functions return a victory flag. | Ultra timer expiry correctly transitions to Victory phase; `saveHighScore` called exactly once; all tests pass | - | cc:完了 |
| 12.1b | **Fix combo off-by-one (TDG §7)** — `state.combo` initializes to `-1` (state.ts:25). First clear: `newCombo = -1 + 1 = 0`, bonus = `50 × 0 × level = 0`. TDG says first clear should award `50 × 1 × (level+1)`. Change init to `0` and update all dependents (combo popup threshold, combo tests, lock.ts popup guard). | First clear awards non-zero combo bonus; combo popup appears on 2nd clear (value ≥ 1); 670+ tests pass | - | cc:完了 |
| 12.2 | **Add exception safety to game loop** — Any throw in `update()` or `renderFrame()` kills `requestAnimationFrame`, permanently freezing the game. Wrap the loop body in try/catch, log the error, and continue. | Game loop survives any exception in update/render; error is logged to console; all tests pass | - | cc:完了 |

## Phase 13: Hardening [tdd:required]

| Task | Content | DoD | Depends | Status |
|------|---------|-----|---------|--------|
| 13.1 | **Fix NaN dt propagation** — `NaN` passes both `dt > MAX_DT` and `dt < 0` checks (NaN comparisons always false). Add `if (isNaN(dt)) dt = 0;` after the existing guards. | NaN dt is caught and zeroed; normal timing unaffected; all tests pass | - | cc:完了 |
| 13.2 | **Fix lowestY giving free lock reset** — `lowestY` initializes to `-1` (lock-delay.ts:41), so the first gravity drop always resets the resets counter (any piece Y > -1). Initialize to the piece's spawn Y instead. | Each game gets exactly 15 lock-delay resets; first gravity drop does not consume a free reset; all tests pass | - | cc:完了 |
| 13.3 | **Fix B2B Tetris Perfect Clear T-spin exclusion** — The `!tSpinResult.isTSpin` guard (scoring.ts:163) prevents 3200 base score for T-spin Tetris Perfect Clears. Remove guard so the 3200 bonus applies regardless of T-spin status when 4 lines + B2B + PC conditions are met. | T-spin Tetris Perfect Clear correctly awards 3200 × (level+1); non-PC B2B logic unchanged; all tests pass | - | cc:完了 |
| 13.4 | **Fix attract restart mode consistency** — `restartAttractGame()` calls `createInitialState()` (no argument, defaults to Marathon). Change to `createInitialState(this.selectedMode)` so restarts respect the selected mode. | Attract mode restarts in the mode the player selected; all tests pass | - | cc:完了 |
| 13.5 | **Fix audio node accumulation** — `activeOscs`/`activeGains` accumulate 176+ nodes per song cycle with no cleanup. In `cleanup()`, clear and null the arrays after stopping nodes. Ensure `stopMusic()` resets accumulated node arrays between song cycles. | Audio node arrays do not grow between song cycles; no audible artifacts; all tests pass | - | cc:完了 |
| 13.6 | **Fix `lastClearWasB2B` dead field** — Remove the unused field from `GameState` type and all initialization sites. It's never read, only declared. | `lastClearWasB2B` removed from type and all constructors; build passes without errors | - | cc:完了 |
| 13.7 | **Fix `onPieceLocked` dead code** — Remove the unused function from `entry-delay.ts`. It's defined and exported but never called. | `onPieceLocked` removed; no build errors; all tests pass | - | cc:完了 |
| 13.8 | **Fix `handleStart` dead code** — Remove the unreachable function from `actions.ts`. The game loop handles Start for Menu/GameOver/Victory/Paused phases before `processAction` is called, so this function is never reached. | `handleStart` removed; all tests pass | - | cc:完了 |
| 13.9 | **Fix hardcoded dimensions in gravity.ts** — Replace literal `10` and `40` with imported `BOARD_WIDTH` and `BOARD_HEIGHT`. | gravity.ts uses symbolic constants; all tests pass | - | cc:完了 |
| 13.10 | **Fix block-out path missing gravityTimer reset** — Add `gravityTimer: 0` to the block-out return in `state.ts`. | Block-out state includes `gravityTimer: 0`; all tests pass | - | cc:完了 |
| 13.11 | **Fix hard drop lock state consistency** — Replace manual literal in `actions.ts:156` with `resetLockState()` call to match the gravity-lock path. | Hard drop lock-out uses the shared `resetLockState()` function; all tests pass | - | cc:完了 |
| 13.12 | **Fix O-piece rotation state** — `tryRotateCW`/`tryRotateCCW` short-circuit for O-piece without updating rotation. While functionally harmless (all shapes identical), add a rotation state update for consistency with other pieces. | O-piece rotation updates `piece.rotation`; all tests pass | - | cc:完了 |
| 13.13 | **Fix createFirstBag defensive check** — Add a guard in `randomizer.ts:66-68` so `bag[-1]` is never accessed if `findIndex` returns -1. | `createFirstBag` handles edge case of missing valid first piece; all tests pass | - | cc:完了 |
| 13.14 | **Fix particle row-key cross-game leak** — Module-level `lastParticleRows` in canvas.ts is never reset between games. Make it part of the effects module with `clearEffects()`. | `lastParticleRows` reset on `clearEffects()`; particles work across multiple game sessions; all tests pass | - | cc:完了 |
| 13.15 | **Enforce minimum cellSize** — `recalcCellSize` can produce 0 in tiny windows, making board invisible and risking division-by-zero. Clamp to minimum of 1. | `cellSize` never drops below 1; all tests pass | - | cc:完了 |
| 13.16 | **Re-enable 180° rotation** — Implement the previously deferred SRS 180° rotation support. Add kick table for 180° rotations, wire into rotation handlers. | 180° rotation works for all piece types with SRS kick tests; all existing tests pass | - | deferred |

## Phase 14: Post-hardening bug fixes [tdd:required]

| Task | Content | DoD | Depends | Status |
|------|---------|-----|---------|--------|
| 14.1 | **Fix hard drop missing screen flash + full SFX** — `handleHardDrop` calls `executeLock` which returns full scoring metadata (linesCleared, tSpinResult, isPerfectClear, needsLevelupSFX, needsTSpinSFX), but the hard drop path in `loop.ts:handleInput` only plays `playSFX("lock")` and never calls `triggerFlash()`. Fix: in the HardDrop case of `handleInput`, after `processAction`, read the returned state's phase + popup info to determine which SFX and flashes to apply, matching `lockActivePiece` behavior. | Hard drops trigger flash on Tetris (white), T-Spin Double+ (yellow), Perfect Clear (white); play SFX for tetris, tspin, clear, levelup matching gravity-lock path; all tests pass | - | cc:完了 |
| 14.2 | **Fix clearedRowIndices stale data in zero-clear path** — `executeLock` (lock.ts:50-52) sets `s.clearedRowIndices` only in the lines-cleared branch. The zero-clear `else` branch (lines 117-151) never assigns it, leaving stale row indices from the previous lock. This causes canvas.ts:258 to render line-clear flash particles on rows that were cleared many locks ago. Fix: add `s.clearedRowIndices = [];` in the else branch. | Zero-clear locks reset `clearedRowIndices`; no stale line-clear flash on non-clear locks; all tests pass | - | cc:完了 |
| 14.3 | **Fix gravity timer not reset on soft drop** — `handleSoftDrop` (actions.ts:124) resets `lockState.timer` but never resets `gravityTimer`. If gravity timer is near threshold (e.g., 950ms of 1000ms), the piece drops immediately after the soft drop moves down, creating a double-drop feel. Fix: add `gravityTimer: 0` to the new state returned by `handleSoftDrop`. | Soft drop resets gravity timer; no immediate gravity drop after soft drop; all tests pass | - | cc:完了 |
| 14.4 | **Remove redundant victory check in hard drop path** — `executeLock` already calls `checkModeVictory` and returns `victoryTriggered` flag. The hard drop path in loop.ts:256-258 has a redundant second `checkModeVictory` call. Fix: check `result.victoryTriggered` instead, or rely on the state's phase transition. The `executeLock` return value is already available in `actions.ts` after the call. | Hard drop victory uses `victoryTriggered` from executeLock; redundant check removed; all tests pass | 14.1 | cc:完了 |
| 14.5 | **Tests for Phase 14 fixes** — (a) hard drop screen flash test: verify `triggerFlash` call count/color matches expectations for Tetris + T-Spin + PC, (b) clearedRowIndices test: after two locks (first clears lines, second does not), verify clearedRowIndices is empty, (c) gravity timer reset test: soft drop produces `gravityTimer: 0`, (d) redundant victory test: mock `checkModeVictory` and verify it's called exactly once per lock. | ≥10 new tests for hard drop flash/SFX, clearedRowIndices, gravity timer reset, victory dedup; all pass | 14.1, 14.2, 14.3, 14.4 | cc:完了 |

## Phase 15: Operational hardening

| Task | Content | DoD | Depends | Status |
|------|---------|-----|---------|--------|
| 15.1 | **Verify deploy workflow after GitHub incident** — The deploy.yml now uses git commands instead of third-party GitHub Actions. The 2026-05-26 GitHub auth incident caused 403 errors on git push. Once the incident is resolved, manually trigger the workflow and verify it deploys successfully. | Workflow completes successfully; game accessible at GitHub Pages URL; all steps pass (test, build, deploy) | - | cc:完了 |
| 15.2 | **Consider Perfect Clear B2B modern rule** — Per 2009 TDG spec, Perfect Clear was NOT B2B-eligible. However, modern Tetris games (Tetris.com, Puyo Puyo Tetris, 2019 TGMP) do count PC as B2B-eligible. If the intent is to match modern conventions, add PC to `isB2BEligible` in scoring.ts. Code is currently correct for 2009 rules. | Decision documented; if changed: PC clears can continue B2B streak; no regression in existing tests | - | cc:完了 |

## Phase 16: New bug fixes [tdd:required]

| Task | Content | DoD | Depends | Status |
|------|---------|-----|---------|--------|
| 16.1 | **Fix music not playing after pause-menu restart** — Pause menu "Restart" sets `prevPhase = GamePhase.Playing` manually (loop.ts:200-204), which masks the GameOver→Playing phase transition. `onPhaseTransition(GamePhase.Playing)` is never called, so `playMusic()` is never invoked. Fix: remove the `prevPhase` assignment on restart; the phase dispatch will detect the transition naturally. | Music plays after pause-menu restart; no regression in other restart paths (GameOver Start, Victory Start); all tests pass | - | cc:完了 |
| 16.2 | **Fix T-spin Mini mislabeled as "T-SPIN SINGLE"** — The popup logic in `executeLock` (lock.ts:90-96) checks `tSpinResult.isTSpin` without also checking `tSpinResult.isMini`. A T-spin Mini single line clear shows "T-SPIN SINGLE" instead of "T-SPIN MINI". Fix: add `isMini` check in the popup label logic to display the correct label. | T-spin Mini shows "T-SPIN MINI" popup; full T-spin Single still shows "T-SPIN SINGLE"; all tests pass | - | cc:完了 |
| 16.3 | **Fix B2B break never displayed to the user** — `isB2BBroken` is computed in `evaluateClear` (scoring.ts:157,174,195) and returned in `ScoreResult` and `LockResult`, but no consumer ever reads it. The B2B chain silently drops without any popup or SFX. Fix: add a "BREAK" or "B2B BROKEN" popup when `scoreResult.isB2BBroken` is true, similar to other popups in `executeLock`. | B2B break shows a popup (e.g., "BREAK" in orange); normal B2B chain continues unaffected; all tests pass | - | cc:完了 |
| 16.4 | **Add direct test suite for executeLock** — `executeLock` (lock.ts, 165 lines) has zero direct test coverage despite handling T-spin detection, line clearing, combo, B2B, Marathon level advancement, popup generation, and victory checking. Create `src/core/lock.test.ts` with: (a) Tetris 4-line clear, (b) T-Spin Single/Double/Triple, (c) T-Spin Mini Single, (d) zero-clear lock (no lines), (e) B2B consecutive Tetris (with and without break), (f) B2B broken by non-eligible clear, (g) Perfect Clear branches, (h) combo chain across clears, (i) Marathon level advancement via effective lines, (j) lock-out path, (k) victory trigger path. | ≥20 direct tests for `executeLock` covering all branches; all pass | - | cc:完了 |
| 16.5 | **Tests for srs.ts, canvas.ts, effects.ts, background.ts** — Add tests: (a) `rotateCW`/`rotateCCW` in srs.ts verify correct rotation state transitions, (b) `renderFrame` in canvas.ts renders without throwing (mock context), (c) `fmtScore` formats all expected score ranges, (d) `triggerFlash` and `spawnClearParticles` in effects.ts don't throw, (e) `renderBackground` renders without throwing, (f) `clearEffects()` resets module state. | ≥15 new tests across srs.ts, canvas.ts, effects.ts, background.ts; all pass | - | cc:完了 |
| 16.6 | **Fix SOFT_DROP_SCORE constant not used** — `SOFT_DROP_SCORE` is defined (constants.ts:423) but `handleSoftDrop` hardcodes `1` instead (actions.ts:119). Replace the literal with the constant. | `handleSoftDrop` uses `SOFT_DROP_SCORE` constant; all tests pass | - | cc:完了 |
| 16.7 | **Remove unreachable code in detectTSpin** — The final `return { isTSpin: false, isMini: false }` (scoring.ts:138-140) is unreachable — all paths return earlier. Remove the dead code. | Unreachable return removed; no build errors; all tests pass | - | cc:完了 |
| 16.8 | **Fix `Cell` type to use `TetriminoType | null`** — `Cell` is `string | null` (types.ts:18), allowing any string in board cells. Change to `TetriminoType | null` so the type system enforces valid piece types. Update any code that depends on the loose string type. | `Cell` typed as `TetriminoType | null`; all code compiles; all tests pass | - | cc:完了 |
| 16.9 | **Fix combo init mismatch in test-utils** — `baseState()` in test-utils.ts:84 initializes `combo: -1` but production `createInitialState` (state.ts:25) initializes `combo: 0`. As a result, any test importing `baseState` and not overriding combo gets a state inconsistent with production. Fix: change to `combo: 0` and verify all consumers still pass. | test-utils baseState combo matches production (0); all tests pass | - | cc:完了 |
| 16.10 | **Remove unused function parameters** — `addHardDropScore` in scoring.ts:215-216 takes an unused `_state: GameState` parameter. `renderEffects` in effects.ts:72 takes an unused `_dpr: number` parameter. Remove both. | Unused `_state` and `_dpr` parameters removed; build clean; all tests pass | - | cc:完了 |

## Phase 17: Hardening and code quality [tdd:required]

| Task | Content | DoD | Depends | Status |
|------|---------|-----|---------|--------|
| 17.1 | **Deduplicate lock-delay reset logic** — The lock-delay timer/reset/onGround calculation is ~70 lines of near-identical code replicated across `handleMove`, `handleRotateCW`, and `handleRotateCCW` (actions.ts:74-99, 189-211, 229-251). Extract to a shared function like `updateLockStateAfterMove(state, newPiece, originalState)` that returns the updated lockState. | Shared function replaces 3 duplicate implementations; all tests pass | - | cc:完了 |
| 17.2 | **Add canvas context guard in main.ts** — `main.ts:5` uses `canvas.getContext("2d")!` with no null check. If the canvas or 2D context fails (headless env, blocked GPU), the `null` propagates and crashes on first render. Fix: add null check and fallback (e.g., render error message or bail). | main.ts handles null canvas/context gracefully; all tests pass | - | cc:完了 |
| 17.3 | **Freeze mutable exported constants** — `SPAWN_POSITION` and `I_SPAWN_POSITION` (constants.ts:12-13) are `const` objects but still mutable via property assignment. Wrap in `Object.freeze()` or use `as const`. Similarly, `GAME_MODES` array (loop.ts:20) should be `as const` or `readonly`. | Exported position objects are frozen; GAME_MODES is readonly; all tests pass | - | cc:完了 |
| 17.4 | **Guard AudioContext construction** — `getAudioContext()` (audio-ctx.ts:8) calls `new AudioContext()` without try-catch. An exception here propagates to callers before their local try-catch blocks. Wrap in try-catch and return null on failure. | AudioContext construction failure returns null; callers handle null gracefully; all tests pass | - | cc:完了 |
| 17.5 | **Add page-unload handler to stop animation loop** — `Game.stop()` cancels the RAF, removes event listeners, and stops audio, but is never called on tab close. Add a `beforeunload` or `pagehide` handler to cleanly stop the game loop on page unload. | Page unload triggers `cancelAnimationFrame`; all tests pass | - | cc:完了 |
| 17.6 | **Add audio module deep tests** — Beyond the existing "does not throw" tests: (a) `getAudioContext` returns null when AudioContext is unavailable, (b) `playSFX` with null context degrades gracefully, (c) `playMusic`/`stopMusic` cycle doesn't leak (verify arrays cleared), (d) audio-ctx.ts `.resume()` on suspended context. | ≥8 new audio tests covering error/degradation paths; all pass | - | cc:完了 |
| 17.7 | **Add offscreen canvas context guard in canvas.ts** — `boardCacheCanvas.getContext("2d")!` (canvas.ts:47) can crash if the offscreen canvas creation fails or context is unavailable. Fix: add null check and fallback (skip caching, render directly). | Offscreen canvas context null guarded; fallback renders without crashing; all tests pass | - | cc:完了 |
| 17.8 | **Fix keyboard.ts non-null assertions** — `this.keys.get(e.code)!.pressed` (keyboard.ts:95,111) uses non-null assertion after a `.has()` check. If the two calls somehow disagree (mutation between them), it crashes. Fix: use `this.keys.get(e.code)?.pressed ?? false` instead. | keyboard.ts uses optional chaining instead of `!`; all tests pass | - | cc:完了 |
| 17.9 | **Replace hardcoded numeric literals with named constants** — `recalcCellSize` in loop.ts:90 caps at hardcoded `36`. Ultra danger threshold in canvas.ts:706 is hardcoded `30000`. Both should be named constants in constants.ts. | `MAX_CELL_SIZE` and `ULTRA_DANGER_THRESHOLD` constants defined and used; all tests pass | - | cc:完了 |
| 17.10 | **Fix double resize handler and cleanup on page unload** — Both main.ts:15 and loop.ts:55 attach `resize` handlers, causing redundant layout reads per resize event. Also, main.ts's listener is never removed. Fix: remove main.ts resize handler (loop.ts already handles it), and consolidate all unload cleanup (17.5) into a single `pagehide` handler that also cleans up the main.ts listener. | Single resize handler; page unload cleans up all listeners; all tests pass | 17.5 | cc:完了 |
(none pending)

## Phase 18: Critical bug fixes [tdd:required]

| Task | Content | DoD | Depends | Status |
|------|---------|-----|---------|--------|
| 18.1 | **Fix Ultra timer expiry during Playing keeps activePiece** — `tickPlayingState` returns `{ ...state, modeTimer: 0, phase: GamePhase.Victory }` but the spread preserves `activePiece`. `updatePlaying` continues calling `applyGravity` and `checkLock` on the Victory state, which can move/lock the piece and play SFX after the game should be over. Fix: add `activePiece: null` in the Victory return of `tickPlayingState`, OR add an early return in `updatePlaying` when `tickPlayingState` triggers Victory. | Ultra timer expiry during Playing sets activePiece=null; no gravity/lock processing after Victory; all tests pass | - | cc:完了 |
| 18.2 | **Fix holdPiece swap path missing gravityTimer reset** — The swap path (state.ts:107-119) spreads `baseState` which includes the old `gravityTimer` value. First-hold path and `spawnFromQueue` both reset gravityTimer to 0 but swap does not. If gravityTimer is near threshold when swapping, the newly swapped piece could drop immediately. Fix: add `gravityTimer: 0` to the swap return object. | holdPiece swap returns gravityTimer=0 matching other spawn paths; all tests pass | - | cc:完了 |

## Phase 19: Cleanup and test coverage

| Task | Content | DoD | Depends | Status |
|------|---------|-----|---------|--------|
| 19.1 | **Remove dead eslint-disable comment** — `src/test-utils/test-utils.ts:114` has `/* eslint-disable @typescript-eslint/no-unused-vars */` but no ESLint is installed or configured in the project. Remove the dead comment. | eslint-disable comment removed; build passes; all tests pass | - | cc:完了 |
| 19.2 | **Remove 6 dead exports in test-utils.ts** — `asciiBoard`, `boardToAscii`, `fillRows`, `randomBoard`, `randomPiece`, and `validSpawnXs` are defined and exported but never imported anywhere. Remove them along with their associated imports. Note: test files like `lock.test.ts` and `integration.test.ts` define their own local `fillRows` — those are unaffected. | 6 exports removed; no remaining imports reference them; build passes; all tests pass | - | cc:完了 |
| 19.3 | **Remove 7 source exports never used outside their file** — `AIControllerConfig` (ai-controller.ts), `WALL_KICKS_JLSTZ`/`WALL_KICKS_I` (constants.ts), `HighScore` (high-scores.ts), `LockResult` (lock.ts), `LastLockResult` (types.ts), `InputCallback` (keyboard.ts) are exported but only referenced within their own file. Remove `export` keyword. | 7 `export` keywords removed; build passes; all tests pass | - | cc:完了 |
| 19.4 | **Add unit tests for main.ts error-handling paths** — `main.ts` has three untested branches: (a) null canvas element renders error message, (b) null 2D context renders error message, (c) `pagehide` handler calls `game.stop()`. Add unit tests by stubbing `document.getElementById` and `HTMLCanvasElement.getContext`, verifying the error path renders the correct DOM content and the cleanup path fires. Note: E2E tests cover the happy path only. | ≥3 tests covering null canvas, null context, and pagehide; all pass | - | cc:完了 |

## Archive

(none)
